const PROD_BASE_URL = process.env.REACT_BASE_URL;

const prod = {
MOBILE-OTP: `${PROD_BASE_URL}/MOBILE/OTP`
}

const PREpROD = {
    MOBILE-OTP: `${PROD_BASE_URL}/MOBILE/OTP`
    }

    const local = {
        MOBILE-OTP: `${PROD_BASE_URL}/MOBILE/OTP`
        }


        const environment = prod;

        export default environment;